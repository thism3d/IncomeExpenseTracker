<?xml version="1.0" encoding="utf-8"?>
<!-- DEBUG BUILDS ONLY.
     Android 9+ blocks cleartext HTTP, so a debug build talking to the dev backend
     over http://10.0.2.2:5051 (the emulator's alias for the host machine) would
     fail with a CLEARTEXT_NOT_PERMITTED error that looks like a network outage.
     This lives in src/debug/, so the release APK keeps HTTPS-only enforcement. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>

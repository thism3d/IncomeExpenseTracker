<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The INTERNET permission is required for development. Specifically,
         the Flutter tool needs it to communicate with the running application
         to allow setting breakpoints, to provide hot reload, etc.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Debug-only: Android 9+ blocks cleartext HTTP, so a debug build talking to
         the dev backend over http://10.0.2.2:5051 (the emulator's alias for the
         host) fails with CLEARTEXT_NOT_PERMITTED — which looks exactly like a
         network outage. The release build does not merge this file, so it stays
         HTTPS-only. -->
    <application android:networkSecurityConfig="@xml/network_security_config" />
</manifest>
